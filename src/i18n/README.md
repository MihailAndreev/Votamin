# Votamin i18n (Интернационализация)

## 📖 Преглед

Votamin използва проста, но мощна i18n система за поддръжка на множество езици. Системата е изградена без външни библиотеки и предоставя лесен начин за добавяне на нови преводи.

## 🌍 Налични езици

- 🇧🇬 **Български (bg)** - по подразбиране
- 🇬🇧 **English (en)**

## 📁 Структура

```
src/i18n/
├── index.js           # Главен i18n модул
├── locales/
│   ├── bg.js         # Български преводи
│   └── en.js         # Английски преводи
└── README.md         # Тази документация
```

## 🚀 Как работи?

### 1. Автоматично засичане на език

Системата автоматично определя езика по следния приоритет:
1. Запазен избор в `localStorage` (`votamin_lang`)
2. Език на браузъра (`navigator.language`)
3. Български (по подразбиране)

### 2. В HTML файловете

Добави `data-i18n` атрибут с ключа на превода:

```html
<h1 data-i18n="home.hero.title">Зареди</h1>
<p data-i18n="home.hero.subtitle">Votamin е платформа за бързи...</p>
```

### 3. В JavaScript файловете

```javascript
import { i18n } from '../../i18n/index.js';

// Вземи превод
const title = i18n.t('home.hero.title');

// След рендериране на HTML, зареди преводите
i18n.loadTranslations();

// Смени езика
i18n.setLang('en');

// Вземи текущия език
const currentLang = i18n.getCurrentLang();
```

## ➕ Добавяне на нова страница за превод

### Стъпка 1: Добави ключове в `locales/bg.js`

```javascript
export default {
  // ...existing code...
  newPage: {
    title: 'Нов заглавие',
    description: 'Описание на нова страница',
    buttons: {
      submit: 'Изпрати',
      cancel: 'Отказ'
    }
  }
};
```

### Стъпка 2: Добави английските преводи в `locales/en.js`

```javascript
export default {
  // ...existing code...
  newPage: {
    title: 'New Title',
    description: 'New page description',
    buttons: {
      submit: 'Submit',
      cancel: 'Cancel'
    }
  }
};
```

### Стъпка 3: Използвай в HTML

```html
<h1 data-i18n="newPage.title">Нов заглавие</h1>
<p data-i18n="newPage.description">Описание на нова страница</p>
<button data-i18n="newPage.buttons.submit">Изпрати</button>
```

### Стъпка 4: Зареди преводите в JS

```javascript
import { i18n } from '../../i18n/index.js';

export default function render(container) {
  container.innerHTML = htmlContent;
  i18n.loadTranslations(); // Важно!
}
```

## 🎯 API Справка

### `i18n.t(key)`
Връща превод по ключ.

```javascript
i18n.t('home.hero.title') // "Зареди" или "Charge up"
```

### `i18n.setLang(langCode)`
Сменя езика и презарежда страницата.

```javascript
i18n.setLang('en'); // Смени на английски
```

### `i18n.getCurrentLang()`
Връща текущия език код.

```javascript
const lang = i18n.getCurrentLang(); // "bg" или "en"
```

### `i18n.loadTranslations()`
Зарежда преводите за всички елементи с `data-i18n` атрибут.

```javascript
i18n.loadTranslations();
```

### `i18n.getAvailableLanguages()`
Връща масив с налични езици.

```javascript
const langs = i18n.getAvailableLanguages();
// [
//   { code: 'bg', name: 'Български', flag: '🇧🇬' },
//   { code: 'en', name: 'English', flag: '🇬🇧' }
// ]
```

## 🔧 Language Switcher

Езиковият превключвател е вграден в navbar компонента и позволява на потребителите да сменят езика с един клик.

```html
<!-- Автоматично налично в navbar -->
<li class="nav-item dropdown">
  <a class="nav-link dropdown-toggle" href="#" id="langDropdown">
    <span id="currentLangFlag">🇧🇬</span>
  </a>
  <ul class="dropdown-menu">
    <li><a class="dropdown-item" data-lang="bg">BG Български</a></li>
    <li><a class="dropdown-item" data-lang="en">UK English</a></li>
  </ul>
</li>
```

## 📝 Best Practices

1. **Винаги запазвай оригиналния текст като fallback:**
   ```html
   <h1 data-i18n="home.title">Оригинален текст</h1>
   ```

2. **Използвай точкова нотация за вложени ключове:**
   ```javascript
   i18n.t('home.hero.title')
   ```

3. **Зареждай преводите След рендериране на HTML:**
   ```javascript
   container.innerHTML = htmlContent;
   i18n.loadTranslations(); // След това!
   ```

4. **Добавяй и двата езика едновременно:**
   Когато добавяш нов превод, винаги обновявай `bg.js` И `en.js`.

## 🐛 Troubleshooting

### Проблем: Преводите не се зареждат

**Решение:** Уверете се, че извиквате `i18n.loadTranslations()` СЛЕД като HTML-ът е вмъкнат в DOM-а.

### Проблем: Виждам ключовете вместо преводи

**Решение:** Проверете дали ключът съществува в `locales/bg.js` и `locales/en.js`.

### Проблем: Страницата не се презарежда след смяна на езика

**Решение:** `i18n.setLang()` презарежда страницата автоматично. Ако не работи, проверете за JavaScript грешки в конзолата.

## 🌟 Бъдещи подобрения

- [ ] Добави повече езици (FR, DE, ES)
- [ ] Имплементирай динамично зареждане на преводи
- [ ] Добави pluralization support
- [ ] Добави формат за дати и числа по локал
- [ ] SEO оптимизация с `<html lang="...">` и `hreflang` тагове

## 💡 Забележки

- Преводите се съхраняват в `localStorage` и се запазват между сесии
- Системата е лека и без зависимости
- Работи с всички съществуващи JavaScript frameworks
- SEO-friendly с правилни `lang` атрибути
